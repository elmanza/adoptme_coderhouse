<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Pets</title>
</head>
<body style="padding: 0px; margin: 0px;">
    <div style="max-width: 500px; width: 100%; margin: 80px auto 0px; border-radius: 8px; border: 1px solid rgb(233, 233, 233);">
        <h1>Lista de mascotas</h1>
        <div>
            <% if (pets.length) { %>
                <ul>
                    <% pets.forEach((pet) => { %>
                        <li><%= pet.name %>, <b><%= pet.specie  %></b></li>
                    <% }) %>
                </ul>
                <% } %>
        </div>
    </div>
</body>
</html>